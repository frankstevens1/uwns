import { baseTokens as tokens } from "../theme";

/**
 * Spinner recipe tokens.
 * Extend deliberately: add size / variant / state only when needed.
 */
export const spinnerTokens = {
  base: {
    // radius: tokens.radius.md,
    // padding: tokens.space[3],
  },

  // size: {},
  // variant: {},
  // state: {},
} as const;
